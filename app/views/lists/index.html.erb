<div class="container">
  <h1>Lists</h1>
    <div>
        <ul>
        <% @lists.each do |list| %>
          <li> <%= link_to list.name, list %></li>
        <% end %>
        </ul>
      </div>
        <%= link_to "Add list", new_list_path %>

    </div>
